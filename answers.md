A) I was meaning the literal network expansion. But feel free to use best practices if you have hard opinion. 2. I was thinking about all three since new info is impacting everything. 3. Yeah, parameters should be limited so that kaggle can accept them
B) I was thinking something like this: try to solve the level using our existing knowledge. If it is possible - good. Otherwise: notice what new features are on this level - go investigate these features (after successfull understanding -> try to solve the level with newly obtained info internally). If we can - proceed live. otherwise - try pass this level imitating the reset - start from beggining given new data. Keeping in mind that our agent has everything they need to pass this level. If they can't - something is off. Compute limit per action should be calculated given the kaggle constraints. Reasoning should work on current level frames, but since our model improved from previous levels - it will be able to use these knowledge
C) dimensions should be robust. Meaning - AI Agent shouldn't focus on hacking these dimensions and they should be genuinly helpful in achieving the goal. Examples are: curiosity, efficiency, intuition, proximity to goal, savvy, and think of any other dimensions and a robust architecture of our reward vector that imitate human approach to solving challenges like arc agi 3. 8) I am thinking hand-designed should be start, I am optimistic enough to believe hybrid would be better - since AI Agent can create a new dimension - this system imitates the parent-child learning process + self-exploration for Agent to build its own beliefs and rules themselves. If we can build a robust hybrid system - let's do it. I thinkg reward vector should be updated every step (even internal) - so that Agent understands whether they are progessing. 
D) We want to update after every new info we get from environment, after level success and after full game win.
11) I believe we will never reach Game_over since we have the reset button. But if we do fail - punish and update.
12) we keep them and we use them to become better
E) I am thinking about one global SAM with per-game adapters.
14. Yes, we will pretrain
15. push toward human baseline and create a new benchmark architecture for evolving AI Agents
F. 64x64 grid.
17) do actions change? if they do - dynamically. Otherwise fixed
G) Yes, first win - is the Agent that can actually solve something. But we want auditable track record so that we know where our agent struggles and what to improve. Our main philosofical approach is the following: we want to address the solution to this challenge based on how human would solve this challenge. And try to apply human best practices to our AI. So if at any point of architectural decisions or implementation you struggle on how human would do anything - ask me. I will help you navigate.
19) Choose the one that suites us most for our prototyping. But I was thinking ls20 to start.